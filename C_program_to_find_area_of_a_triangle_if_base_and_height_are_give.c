#include<stdio.h>
int main(){
int base,hight,area;
printf("enter the base of the tringle:");
scanf("%d",&base);
printf("Enter the hight of the teingle:");
scanf("%d",&hight);
area=(base*hight)/2;
printf("the of tringle is=%d",area);

return 0;
}